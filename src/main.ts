/**
 * Placeholder entry point.
 *
 * The Effect application is built up in Phase 3 onwards — see
 * `docs/migration/05-phased-checklist.md`. Until then this file exists only so
 * `npm run typecheck` and `npm run build` have something to compile.
 */
console.log('not yet implemented');
